enum Constants {
  BEARER_PREFIX = "Bearer ",
  POLICY_VERSION = "2012-10-17",
  PRINCIPAL_ID = "user",
  ALLOW = "Allow",
  DENY = "Deny"
}

enum Envs {
  JWT_SECRET = "JWT_SECRET"
}

export default { Constants, Envs };
